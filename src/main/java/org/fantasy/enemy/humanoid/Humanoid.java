package org.fantasy.enemy.humanoid;

import org.fantasy.enemy.Enemy;
import org.fantasy.enemy.EnemyType;

public class Humanoid extends Enemy {
    {
        setEnemyType(EnemyType.HUMANOID);
    }
}
